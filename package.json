{
  "name": "unykorn",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:water": "hardhat run scripts/deployWaterVault.js --network polygon",
    "deploy:carbon": "hardhat run scripts/deployCarbonVault.js --network polygon",
    "deploy:stablecoin": "hardhat run scripts/deployStablecoin.js --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
